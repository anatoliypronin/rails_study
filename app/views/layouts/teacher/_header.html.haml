%nav.navbar.navbar-expand-lg.navbar-dark.bg-dark
  .collapse.navbar-collapse
    .navbar-nav
      = link_to t('header.home'), teacher_root_path, class: 'navbar-brand'
      = link_to t('header.courses'), "#", class: 'nav-item nav-link'
      = link_to t('header.lessons'), teacher_lessons_path, class: 'nav-item nav-link'
      = link_to t('header.home works'), "#", class: 'nav-item nav-link'
      = link_to t('header.articles'), teacher_articles_path, class: 'nav-item nav-link'
      = link_to t('header.personal_account'), edit_teacher_profile_path, class: 'nav-item nav-link'
      %ul.navbar-num
  %div
    %div{style: 'color: white'}= current_teacher.email
    %div= link_to t('header.exit'), teacher_session_path, method: :delete, class: 'btn btn-outline-secondary btn-sm'